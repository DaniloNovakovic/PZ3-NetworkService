<UserControl x:Class="PZ3_NetworkService.Views.ReactorCellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PZ3_NetworkService.Views"
             xmlns:viewModel="clr-namespace:PZ3_NetworkService.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="150">
    <UserControl.DataContext>
        <viewModel:ReactorCellViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Row="0" Grid.ColumnSpan="2" BorderThickness="2" BorderBrush="{Binding BorderBrush}">
            <Image Source="{Binding SelectedReactor.Type.ImgSrc}"/>
        </Border>
        <Button Grid.Row="1" Grid.Column="0" Content="Remove" Command="{Binding UntrackCommand}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Temperature}" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
